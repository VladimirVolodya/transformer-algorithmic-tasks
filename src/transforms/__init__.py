"""No instance/batch transforms are used for the addition task.

The template's data pipeline still expects the ``transforms`` config group to
exist; see ``src/configs/transforms/empty.yaml`` for the no-op configuration.
"""
