_target_: src.logger.WandBWriter
project_name: "transformer-algorithmic-tasks"
entity: null            # set to your wandb team/user via CLI override or env; never hardcode
run_name: "addition_run" # overridden per PE variant by scripts/train_all_variants.sh
mode: "offline"         # offline: logs locally to ./wandb, no account needed.
                        # For live dashboards: `wandb login` (or set WANDB_API_KEY) then
                        # override with writer.mode=online (see README).
loss_names: ["loss"]
log_checkpoints: False  # if True, uploads checkpoints to W&B (online mode)
id_length: 8
